procedure run(channel ch, int n1, int n2)
    par
        ssend(n1, ch)
    and
        srecv(n2, ch)
    rap

procedure main()
    channel ch
    int n1
    n1+=5
    int n2
    call run(ch, n1, n2)
    uncall run(ch, n1, n2)